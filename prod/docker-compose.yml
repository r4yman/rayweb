version: "3.7"

services:
        web:
                image: nginx:latest
                ports:
                        - "80:80"
                volumes:
                        - ./site.conf:/etc/nginx/nginx.conf
                networks:
                        - webapp
                depends_on:
                        - python

        python:
                tty: true
                volumes:
                        - ./:/rayweb
                image: python:latest
                build:
                        context: .
                networks:
                        - webapp

networks:
        webapp:
